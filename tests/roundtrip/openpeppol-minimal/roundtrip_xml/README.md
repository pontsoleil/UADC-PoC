# Round-Trip XML

This directory contains UBL Invoice XML regenerated from the minimal structured
CSV.

It is used as the smallest round-trip check for XML reconstruction from the
LHM syntax binding.
