# Structured CSV

This directory contains the structured CSV generated from the minimal
OpenPeppol/UBL Invoice source XML.

The file is used to verify the basic XML-to-LHM conversion shape before running
larger BIS Billing example checks.
