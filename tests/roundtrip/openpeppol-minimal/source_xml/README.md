# Source XML

This directory contains the minimal OpenPeppol/UBL Invoice XML used as a small
baseline round-trip input.

It is intentionally kept separate from generated artifacts so that source and
round-trip XML can be compared directly.
