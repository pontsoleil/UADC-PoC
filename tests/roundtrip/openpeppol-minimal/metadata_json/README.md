# Metadata JSON

This directory contains xBRL-CSV JSON metadata generated for the minimal
OpenPeppol/UBL Invoice structured CSV.

The metadata is used for Arelle-oriented validation of the structured CSV
against the generated `plt-oim` taxonomy.
