# Round-Trip XML

This directory contains UBL Invoice XML regenerated from the BIS Billing
structured CSV files.

These files are compared with the source XML for round-trip review and are
validated with an XML parser or UBL 2.1 schema validation where available.
