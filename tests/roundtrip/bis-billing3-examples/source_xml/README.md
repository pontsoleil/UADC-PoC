# Source XML

This directory contains the BIS Billing example UBL Invoice XML files used as
round-trip test inputs.

Files here are the starting point for syntax binding conversion. They should
remain distinguishable from generated structured CSV and regenerated XML so
that reviewers can compare the original instance with the round-trip result.
