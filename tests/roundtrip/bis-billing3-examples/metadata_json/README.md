# Metadata JSON

This directory contains xBRL-CSV JSON metadata generated together with the BIS
Billing structured CSV files.

The metadata must point to the generated OIM taxonomy entry point under
`out/taxonomy/plt/plt-oim-2026-07-05.xsd` when regenerated locally.
