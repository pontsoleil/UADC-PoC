# Structured CSV

This directory contains structured CSV files generated from the BIS Billing
example source XML files.

Each CSV is produced from the LHM syntax binding and carries BG dimension
columns plus BT fact columns. These files are review artifacts for confirming
the hierarchical tidy data representation.
