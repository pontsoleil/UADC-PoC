# Samples Directory

This directory contains small committed sample files used by lightweight tests
and examples.

## Subdirectories

- `input/` - Source XML samples used by conversion tests.
- `expected/` - Stable expected outputs used by focused regression checks.

Larger generated artifacts belong under `out/` or `tests/roundtrip/`, depending
on whether they are temporary generated output or reviewable test evidence.
