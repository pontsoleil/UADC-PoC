# 決定: バインディング表 をランタイム権限にする

## コンテンツ

初期コンバーターのドラフトはLHM CSVを読み込み、互換性のフォールバックカラムを使用してStructured CSVの列を解決し、行スコープを繰り返すことができます。 これにより、ランタイムの挙動は、アクティブなバインディング表に依存し、1つのCSVを検査することで、コンバージョンが何をするのかを調べるのが難しくなります。

PoC は、操作とレビューに十分な明示的である バインディング表 を必要としています。 構文バインディング と 意味バインディング: バインディング表 は、コンバータで必要なモデル情報を運ぶ必要があります。

## デコレーション

1. 要求しない**--lhm-csv**業務用 構文バインディング または 業務用
結合の転換。
2. 行スコープ のランタイムの権限として活動的な結合 CSV を、扱います
意味パスs、ソース列、およびターゲットパス。
3. お問い合わせ**タイプ=C**semanticクラスと**タイプ=A**事実や
ターゲットフィールド。
4. 使用条件**multiplicity**お問い合わせ**ツイート**クラスが繰り返されるかどうかを判断する行。
5. で 意味バインディング, 導きます**ソース 列**から**semantic_path**そして導きます
繰り返し 行スコープ から**ツイート**同じバインディング表の行。
6. たとえばランタイムのみの派生したカラムを保持しない**ソース 列**,
**リピート グループ パス**または**リピート group column**意味バインディング CSVファイルで。
7. 未使用の構文バインディング列を削除します**パス**そして、**省略 path**.

## コンシーケンス

- レビュアーは、バインディングを調べてランタイム行適用範囲動作を理解することができます
テーブルだけ。
- 意味バインディング ファイルには、ターゲット定義の列と
**semantic_path**, **タイプ:**と**multiplicity**.
- 構文バインディング ファイルには、LHM - 実行時に必要な行列が含まれます。
含まれるもの**構造 csv level**, **構造 csv column**, **エレメント**, **xpathの**, **タイプ:**と**multiplicity**.
- LHM は、ガバナンスのソースが残っていますが、運用スクリプトは必要ありません
通常の変換を実行するためにそれをロードします。
- バインディング表 の更新には、注意が必要です: 繰り返し対象の動作は、
正しい**ツイート**行と行**multiplicity**値。
