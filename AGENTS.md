# UADC-PoC 正式GIT個別作業指示

更新日: 2026-09-09


## 作業場所と本規定の適用

本ファイルの配置先は、正式GITの`AGENTS.md`である。

- 開発・修正・生成・テスト及び記録の作成先: `C:\Users\nobuy\GitHub\WORK\UADC_PoC`
- 受入済み成果物の正式反映及び承認済みGit操作の対象: `C:\Users\nobuy\GitHub\GIT\UADC-PoC`

正式GITは、受入済みWORK成果物を反映するチェックアウトである。本書に残る開発、修正、生成、機能・画面・帳票テスト、一時作業、報告書及び`CURRENT_BASELINE.md`・`HANDOFF.md`・`DECISIONS.md`更新の記述は、すべて対応するWORKで実施する。これらの相対パスもWORKを基準とする。正式GIT内で直接編集・生成・テストを行わない。WORKを指す既存の適用対象・共通指示は、正式GIT版でも開発先及び参照規定として保持する。

末尾の9条項を正式GIT運用に適用する。親規定の一般的なステージ手順省略規定又は「改訂ブランチへ接続・反映」等の表現によって、WORKでの検証・受入・コピー計画を省略しない。矛盾がある場合は箇所を報告して停止する。

同一の入力、コード、設定、依存バージョン、対象成果物、検証範囲で受入済みの試験は再利用する。2回生成等の既存試験項目は、新たに検証が必要な場合の要件であり、完了済み試験の無条件再実行を要求しない。再実行時は前提の実質的な差分と理由を記録する。

機能の合格と手順遵守の合格を区別する。各段階で必要な条件を、実行者及び審査者が実行・許可前に確認する。コピー後のSHA一致を確認する前にステージ等の後続段階へ進まない。規定の追記自体を、技術的な書込み禁止の実装又は有効性確認完了と扱わない。

正式GITでの文書反映にも、既存の文書公開制限と対象パスの明示承認を適用する。「終わり」等の指示は正式GITでの直接更新又はコピーの追加承認ではない。ChatGPTへの一時的なGIT読取り許可を恒久的な操作権限に拡張しない。

日本語を本規定及び報告の記述言語とし、パス、コマンド、識別子、規格名、正式名称は原表記を保持する。

## 指示の関係

本書はUADC-PoCの正式GITチェックアウトに適用する。管理されたWORK/GIT環境で操作する際は、適用される親規定、WORK共通規定及びUADC_PoC個別規定も全文読み、本書と整合させて適用する。

指示が衝突する場合は、ユーザーの現在の明示指示及び最も近い適用対象の`AGENTS.override.md`又は`AGENTS.md`を確認する。削除、上書き、公開、Git、本番の範囲が変わる矛盾については、箇所を示して停止し、確認する。

末尾の9条項を本プロジェクトの正式GIT運用に適用する。一般的なステージ手順の省略規定や「改訂ブランチへ接続・反映」という表現から、WORK工程の省略又は正式GITでの直接開発を許可されたと解釈しない。

## プロジェクト

このチェックアウトは、受入済みWORK成果物の正式反映及び承認されたGit操作のための場所である。

GitHubリポジトリ:

```text
https://github.com/pontsoleil/UADC-PoC
```

プロジェクトは、次の概念実証を開発・保守する。実際の開発・保守は対応するWORKで行う。

- EN 16931インボイス論理階層モデル（LHM）
- UBL構文結合
- XMLから階層CSVへの変換
- 階層構造化CSV
- xBRL-CSVメタデータ
- タクソノミ生成
- XMLの往復再構築試験

本プロジェクトは以前UADAリポジトリで開発され、その後UADC-PoCへ改称された。

## 作業ディレクトリ

開発・修正・生成・テストは次のWORKで行う。

```text
C:\Users\nobuy\GitHub\WORK\UADC_PoC
```

Gitクローン先及び正式反映先:

```text
C:\Users\nobuy\GitHub\GIT\UADC-PoC
```

Gitリモートは次を指すことを確認する。

```text
https://github.com/pontsoleil/UADC-PoC.git
```

旧開発ディレクトリをGitHub同期先へ転用しない。旧ディレクトリには、無関係のローカル履歴、大きなファイル、旧作業領域が含まれる可能性がある。

WORKの場所又は対応関係を確認できない場合は停止する。正式GITを代替の開発場所にしない。

## リポジトリの重要規則

- 明示依頼なしに`out/`のローカル生成物をコミットしない。
- Pythonキャッシュ、仮想環境ディレクトリをコミットしない。
- 90 MBを超えるファイルをコミットしない。
- 秘密鍵、認証情報、証明書、パスワード、ローカル証明書、端末固有設定をコミットしない。
- 明示依頼なしに旧UADAフォルダーを再導入しない。
- ソース、テスト、仕様を現行UADC-PoC構成で保持する。
- `.gitattributes`に従い、UTF-8テキストとLF改行を保持する。CSV及び外部原本の扱いは適用されるWORKの文字コード・改行規則も確認し、矛盾を無断で正規化して解消しない。
- 別のWORKツリーを正式GITへミラー又は再帰同期しない。`robocopy /MIR`、無指定の再帰コピー又は同等の全体同期を使わない。
- WORKからの反映は、相対コピー元・先、操作、WORKのSHA-256、変更前Git状態及びSHA-256、結果SHA-256を記録したファイル単位の`COPY_PLAN`を使用する。
- 未追跡・未コミットのユーザー変更を保護する。配置先が承認済みWORK原本及び想定Git基準の両方と異なる場合、上書きせず停止する。
- コピーとGit操作は別承認とする。編集又はコピーの依頼は、ステージ、コミット、タグ、ブランチ、プッシュ、プルリクエスト操作を承認しない。
- 現在の依頼で実対象が明示されない限り既存ファイルを削除・改名しない。再編自体は削除承認ではない。
- プロジェクト全体の`README.md`と`README.pdf`を保持する。公開配置作業で削除又は機械的に上書きせず、置換は個別に確認する。

## 機密・非公開データ

次をGit、GitHub、公開文書、ログ又はチャットへ追加しない。

- 実際の会計、仕訳、請求、支払、税務データ
- 実データに由来する顧客、仕入先、銀行口座、カード、個人名、住所、摘要
- 実際の取引先、口座又はカードデータから生成したマスタ
- 認証情報、接続設定、秘密鍵、証明書、`.env`、端末固有設定
- 非公開README、購入した標準文書、再配布権限が不明な資料
- 公開成果物として個別承認されていないローカル表計算ファイル、生成レビュー結果、ログ、バックアップ、検証報告書

WORKで行うリポジトリ関連試験には、合成データ又は別途承認済みの公開サンプルを使用する。匿名化後も合理的に再識別できる情報を残さない。

## 通常の無視対象パス

次は通常、未追跡のままにする。

```text
out/
__pycache__/
.pytest_cache/
.venv/
venv/
*.pyc
```

## 主なソース領域

編集・生成・テストの相対パスは、対応するWORKを基準とする。各ディレクトリの用途は次のとおり。

```text
docs/        プロジェクト文書・設計記録
references/  参考記録・原本参照
samples/     入力・期待サンプル
specs/       LHM・構文結合仕様
src/         主な変換プログラム
tests/       回帰試験・試験成果物
tools/       LHM・結合・定義・タクソノミ支援用補助スクリプト（ローカルタクソノミ生成器を含む）
```

## 開発方針

対応するWORKでコードを変更する際は、次に従う。

1. 編集前に既存設計を理解する。
2. 小さく、対象を絞った変更を優先する。
3. LHM、結合CSV、変換コード、テストの整合を保つ。
4. 動作変更時は文書を更新する。
5. 明示依頼がない限り、生成成果物を原本定義から分離する。
6. 必須の外部依存が使えない場合は前提を明示する。テスト用のタクソノミ生成器はWORKの`tools/taxonomy/xBRLGL_TaxonomyGenerator.py`にある。通常動作を外部の`XBRL-GL-2026`チェックアウトへ依存させない。

三つの結合方式を区別する。

- 構文結合はXPath、JSONPath、CSV位置等の物理構文で値を取得する。
- 意味対応表は入力項目をHMDの`semantic_path`へ対応付ける。
- 平坦CSVは、必要に応じ出現選択子を含む`semantic_path`を列識別子に使用する。`flat_semantic_path`を別概念として導入しない。

原本固有の結合規則を変換ロジックへ無断で直接記述しない。列、選択子、条件、変換、既定値、エラー処理は、適用する結合表又は明示された共通関数で管理する。

## 正規処理経路の事前確認と逸脱防止

- 個別依頼に列挙がなくても、作業開始前に適用される `AGENTS.md`、仕様書、設計判断、プログラム外部仕様、登録済み定義、既存の実行手順・受入記録を確認し、その有効な規定に従う。依頼に規定への言及がないことを、規定の解除又は実装・変換方法の変更許可と解釈しない。
- 変換・生成は正規に登録されたスクリプト、Binding・mapping・設定及び対応する入力を使用する。参考資料の提供だけを根拠に、正規経路を独自処理へ置き換えない。
- 正規経路で対応できない場合は、該当する仕様・実装、不足する定義、判断根拠及び変更案を報告する。最終成果物を生成する独自スクリプト又はその専用対応表を作成する前に、ユーザーの明示許可を得る。
- テストは正規処理の品質と再現性を検証する。独自の補完・迂回処理で生成した成果物を正規処理の検証成功と扱わない。実際に使用したスクリプト・定義・入力・実行条件を記録し、同梱しただけの未使用ファイルと区別する。正式GIT反映時は、受入証跡が反映対象のスクリプト・定義・生成経路に対応することを確認し、独自処理の結果を正規経路の受入根拠へ流用しない。

## テストコマンド

すべて対応するWORK内で実行し、正式GITでは実行しない。入力、コード、設定、依存バージョン、対象成果物、検証範囲が実質的に同じ受入済み試験は再利用する。前提が変わった場合に、変更に必要な範囲だけを検証し、再実行理由を記録する。

広範な確認が必要な場合の既存コマンド:

```text
python -m pytest
```

対象を絞った既存コマンド:

```text
python .\tests\test_lhm_semantic_paths.py
python .\tests\test_lhm_hierarchical_csv_layout.py
python .\tests\test_syntax_binding.py
python .\tests\test_openpeppol_invoice_conversion.py
python .\tests\test_syntax_binding_reverse.py
python .\tests\test_roundtrip_artifacts.py
python .\tests\test_xbrlgl_generator_uadc_lhm.py
```

Arelle又はUBLスキーマ検証をローカルで利用できない場合、成功と推定せず明示して報告する。

## コミット前の確認

現在のユーザー依頼が明示承認するGit操作だけを行う。承認済みコミット前に、WORKの検証・受入記録、`COPY_PLAN`、コピー結果のSHA一致を確認する。正式GITではGitルート、ブランチ、リモート、上流、作業ツリー、差分、ステージ内容を確認する。

```text
git status
git diff --stat
git diff
```

大きなファイルも読み取りで確認する。

```powershell
git ls-files | ForEach-Object {
  $f = $_
  if (Test-Path $f) {
    $item = Get-Item $f
    if ($item.Length -gt 90MB) {
      [PSCustomObject]@{
        File = $f
        MB = [math]::Round($item.Length / 1MB, 2)
      }
    }
  }
}
```

90 MBを超えるファイルが表示された場合はコミットしない。

## コミット・プッシュ方針

コミットメッセージは簡潔な日本語で記述する。

例:

```text
LHM生成規則を更新
UBL構文結合変換を修正
XML往復変換の回帰試験を追加
タクソノミ生成文書を更新
```

WORKで必要な試験・確認・受入を完了し、限定コピーとSHA一致を確認した後、ステージ、コミット、プッシュのそれぞれが明示承認された場合に限り、承認パスを個別に処理する。下記の山括弧部分は承認された実値へ置き換え、`main`を既定の送信先にしない。

```text
git add -- <承認済みパス1> <承認済みパス2>
git diff --cached
git commit -m "簡潔なコミットメッセージ"
git push <承認済みリモート> <承認済みブランチ>
```

`git add .`、`git add -A`を使わない。強制プッシュ、履歴書換え、無断リベース、`git reset --hard`を行わない。

## 報告方針

終了時に、次を日本語で報告する。報告書の作成・更新はWORKで行う。

- 変更内容及び変更ファイル
- 実行した試験、失敗又は省略した試験
- コミット・プッシュの実施有無
- WORK、正式GITチェックアウト又は本番のどこが変わったか
- WORK反映時の承認済みコピー元・先及びSHA-256照合結果
- 該当する結合方式・結合表、入出力形式・件数、実データ・合成データの区別、往復変換・決定性結果
- 未対応項目、値欠落、意味差、Git又は公開から除外した機密ファイル
- Git操作が承認されていた場合のブランチ、コミットID、プッシュ先
- 残作業及び次の安全な開始点

## 操作終了時の安全チェックポイント

承認された操作単位の終了時に、実際の変更、実施・省略した検証、Git・公開状態、機密除外、既知の問題、残作業を確認する。基準、引継ぎ又は継続的な設計・運用判断が実際に変わった場合だけ、対応するWORK内の必要な文書を更新する。

```text
docs/CURRENT_BASELINE.md
docs/HANDOFF.md
docs/DECISIONS.md
```

読取り調査だけで事実が変わらない場合は更新しない。`CURRENT_BASELINE.md`は正式基準が変わった場合だけ更新する。WORKのレビュー成果物をGit基準と扱わない。

ユーザーの単独の「終わり」又は同等の指示は、未完了でもこの確認を要求する。必要な読取り確認及びWORK内の3記録文書の最小限更新だけを認め、コピー、移動、改名、削除、再生成、外部接続、Git操作、公開、本番変更を追加承認しない。正式GIT内で文書を直接更新しない。

これらの文書を正式GITへ反映する場合は、既存の`docs/**`公開制限、対象パスの明示承認、WORK受入及び限定コピー手順に従う。

確認を完了できない、機密混入の可能性がある、又は実状態を確定できない場合は、完了とせず停止して理由を報告する。

## 正式GITにおける作業制限とWORKからの反映

1. **開発・修正・テストはWORKで行う。**
   正式GITでは、ソースコード、設定、データ、文書その他のリソースの開発・修正・生成、および機能テスト・帳票生成・画面テストを行ってはならない。必要な作業はWORKで実施する。

2. **WORKで検証・受入済みのリソースだけを反映する。**
   正式GITへのコピー元は、WORKで必要なテストまたは内容確認を完了し、受入済みとなったリソースに限る。受入後に変更されたリソースは、その変更に必要な検証・受入を完了するまでコピーしてはならない。

3. **操作前に実際の書込先を確認する。**
   作業ディレクトリだけでなく、編集対象、コピー先、生成先、一時ファイル・ログ等の出力先を、解決済みの絶対パスで確認する。WORKから実行する場合も、正式GITへの開発・修正・テストに伴う書込みは禁止する。

4. **ファイル単位のコピー計画とSHA照合を必須とする。**
   コピー前に、WORKの検証・受入記録と、コピー元、コピー先、操作、受入済みWORKのSHA-256、コピー先の変更前SHA-256を記載したコピー計画（`COPY_PLAN`）を確認する。新規ファイルは変更前を「不存在」と記録する。承認された対象だけを限定コピーし、コピー後のSHA-256が受入済みWORKと一致することを確認する。既存ファイルを置換する場合は、変更前のバイト列をバックアップする。全体コピー、ミラー同期、対象外ファイルの変更・削除は禁止する。

5. **承認範囲を拡大解釈しない。**
   ブランチの使用、GITへの反映、ステージ、コミット、プッシュの承認は、正式GIT内での直接開発・修正・テストの承認を含まない。「改訂ブランチへ接続・反映」等の指示によってWORKでの検証・受入工程を省略してはならない。各操作は明示された承認範囲内で行う。

6. **条件を確認できない場合は後続作業を停止する。**
   操作実行者および操作を許可する審査者は、該当する操作を実行・許可する前に、本条項への適合を確認する。作業先、検証・受入記録、コピー計画、SHA一致、承認範囲のいずれかを確認できない場合、または指示が本条項と矛盾する場合は、後続作業を行わず、不足事項または矛盾箇所を報告する。

7. **問題の修正はWORKへ戻して行う。**
   正式GITで問題を発見した場合も、その場で修正してはならない。WORKで修正し、必要な検証・受入とコピー計画の更新を完了した後、対象リソースを再度限定コピーする。

8. **正式GITで許可する操作を限定する。**
   正式GITでは、読み取り調査、承認された限定コピー、コピー結果のSHA照合、差分・ステージ内容・コミット・リモート状態の確認、および承認されたGit操作を行うことができる。これらの確認を理由として、機能テスト、帳票生成、画面テストその他の開発作業を実施してはならない。

9. **ファイルのバイト列とGit正規化後のバイト列を区別する。**
   WORKファイル、正式GITの作業ツリーファイル、ステージ済み内容、コミット済み内容のSHAを区別して記録する。改行変換その他のGit変換が発生する場合は、適用規則と変換前後のSHAを記録し、その変換が受入条件で認められていることを確認する。正規化後の内容一致を、生のバイト列のSHA一致として報告してはならない。
